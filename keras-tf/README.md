## Using Python Notebooks:
https://www.youtube.com/watch?v=suAkMeWJ1yE

## Using Virtual Environments in Python:

python -m venv ./venv
chmod +x ./venv/bin/activate
source ./venv/bin/activate

## Building a model:
1. Data formatting for both X and y labels for keras fit function must be same
2. Keep data in a form that will be easy for the model to use

